import { BoardState, Location } from "./types.ts";

export function getValidMoves(board: BoardState, position: Location): Location[] {
  return [];
}

export function isValidMove(board: BoardState, from: Location, to: Location): boolean {
  return true;
}
